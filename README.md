govlab-academy-gallery
======================

Project gallery for the govlab-academy.

The content comes from a CSV file.

The output is generated in `/gen` from the `generate.py` Python script.

To generate the site, simply run:
`python generate.py`.
